# GitTube

Official GitTube catalog.

GitTube is a Minecraft DeviceOS-style video platform powered by GitHub raw files.

## Structure

- `channels.txt` lists all channels
- each channel has `channel.txt`
- each channel has `videos.txt`
- each video has its own folder with `video.txt`, thumbnail, gif, and optional sound
